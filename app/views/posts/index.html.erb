<section class="page-container">
  <%= render "shared/user_headline", user: @user %>
  <ul class="posts-list">
    <% @posts.each do |post| %>
      <%= render "shared/post_card", post: post, user: @user,  comments: true %>
    <% end %>
  </ul>
  <button class="button">pagination</button>
</section